#ifndef GAME_MAIN_H
#define GAME_MAIN_H

// This method will be called once when initializing the game
void initGameElemets();

// This method will be called on every frame of the game when updating the game state
void gameUpdate();

// This method will be called on every fram of the game when handling events
void gameEvents();


#endif // GAME_MAIN_H